This directory ("controllers") should store all of the API controllers using the 'tsoa' library.
This library provides the Controller class which should be used to extend all of The API controller classes.
These classes will define the API routes, available endpoints and resources exposed.
